<h1>Convert <%= @original_currency %></h1>

<p>Go <a href="/">back</a>.</p>

<ul>
  <% @parsed_data.fetch("currencies").keys.each do |currency_code| %>
    <li>
      <a href="/<%= currency_code %>">Convert 1 <%= currency_code %> to <%= currency_code %>...</a>
    </li>
  <% end %>
</ul>
